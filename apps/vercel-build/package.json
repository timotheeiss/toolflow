{
  "name": "@toolflow/vercel-build",
  "private": true,
  "type": "module",
  "dependencies": {
    "@toolflow/build-worker": "workspace:*"
  },
  "scripts": {
    "build": "node -e \"console.log('Dependencies built; Vercel will bundle the build function.')\"",
    "lint": "eslint api",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  }
}
